import { ChartBarActive } from "./bar-chart-active";
import { CustomRoughBar } from "./custom-rough-bar";

const Chart = {
  BarActive: ChartBarActive,
  RoughBar: CustomRoughBar,
};

export default Chart;
